import React from 'react';
import './Error.css';

// eslint-disable-next-line no-empty-pattern
const HomePageError = ({}) => {
  return (
    <div id="error">
      <h1 className="notFoundTitle">Oops! This page can’t be found ...</h1>
    </div>
  );
};
export default HomePageError;
