import React from 'react';
import { storiesOf } from '@storybook/react';
import HomePageError from './Error.component';

storiesOf('HomePageError', module).add('HomePageError', () => (
  <HomePageError />
));
